-- Пропуски в записи: айфон выключает микрофон свёрнутому приложению, и
-- браузер присылает, где и насколько. Нужны модели — отметить пропуск в
-- конспекте, а не сшить его края — и экрану записи.
-- [{"at_sec": 1390, "dur_sec": 250}, ...]; писатель — модуль notes.
ALTER TABLE recordings ADD COLUMN gaps JSONB NOT NULL DEFAULT '[]';
